#!/usr/bin/env bash
set -euo pipefail
HERE="$(cd "$(dirname "$0")" && pwd)"
# shellcheck disable=SC1091
source "$HERE/_lib.sh"

if should_remote yarn "$@"; then
  exec "$(molt_bin)" run yarn "$@"
fi
REAL="$(real_bin yarn)" || { echo "yarn not found on Mac" >&2; exit 127; }
exec "$REAL" "$@"
